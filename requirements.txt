# PillSafe — Python Dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Core
opencv-contrib-python==4.8.1.78   # OpenCV with LBPH face recogniser
numpy>=1.24.0

# Camera (Raspberry Pi — pre-installed on Pi OS)
# picamera2  # Already available on Raspberry Pi OS

# Hardware interfaces
#RPi.GPIO>=0.7.1                   # GPIO control for servo, buzzer, IR sensors
smbus2>=0.4.3                     # I2C communication with DS3231 RTC

# Serial communication
pyserial>=3.5                     # UART control of SIM800L GSM module

# Web framework
Flask>=3.0.0                      # REST API server

# Configuration
PyYAML>=6.0                       # config.yaml loader

# Scheduling (optional — using custom polling loop instead)
# APScheduler>=3.10.0
